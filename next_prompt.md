# RESTORE & FIX: NUTRIENT COLORS AND CARD DATA

Мы успешно вернули счетчик и данные в карточку. Остался последний штрих: цвета нутриентов стали однообразно-бирюзовыми. Это произошло из-за слишком упрощенной инструкции для ИИ.

## ТВОИ ЗАДАЧИ:

### 1. Исправь `apps/api/src/ai/src/ai.controller.ts`
В `systemPrompt` (инструкция по тегам `<nutr>`) верни ИИ право использовать специфичные типы для цветовой кодировки.
- **ИНСТРУКЦИЯ**:
  `Для тегов <nutr> используй специфичные типы, если они известны: type="iron" (Железо), type="calcium" (Кальций), type="magnesium" (Магний), type="vitamin_c", type="vitamin_d", type="vitamin_b" (B6, B12, Фолаты), type="omega" (Омега-3). Для остальных используй type="marker".`
- Это касается и текста, и технического блока `micro`.

### 2. Исправь `apps/web/src/components/diary/food-log-parser.ts` (если еще не исправлено)
Убедись, что регулярное выражение для значений поддерживает пробелы:
```typescript
const valueMatch = content.match(/([\d.,]+\s*[%A-Za-zА-Яа-я]+)[\s)]*$/);
```

**БОЛЬШЕ НИЧЕГО НЕ МЕНЯЙ.** Не трогай `isMounted`, не трогай таймзоны. Нам нужно только вернуть цвета «как было».

## ПРОВЕРКА:
1. Запиши блюдо с кальцием и железом.
2. Слово «кальций» в тексте должно стать желтым, а «железо» — красноватым (или другими цветами из `nutrientColors.ts`).
3. В карточке точки рядом с этими нутриентами должны соответствовать этим цветам.

Использованные скиллы: surgical-bugfix, prompt-engineering-patterns, color-system-matching
